#include "main.h"

/**
 * mul - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply with the first
 * Return: results of the multiplication of a parameter and b parameter
 */

int mul(int a, int b)

{

	return (a * b);
}
